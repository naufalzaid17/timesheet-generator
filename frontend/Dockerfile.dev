FROM oven/bun:alpine

WORKDIR /app

# Copy dependency configs (lockfile optional via glob for reproducible installs)
COPY package.json bun.lock* ./

# Install development dependencies using Bun
RUN bun install

EXPOSE 3000

ENV PORT=3000
ENV NODE_ENV=development

CMD ["bun", "run", "dev"]
